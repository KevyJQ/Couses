# Couses

## Descripcion 
```shell
Aqui pondre los materiales que fui realizando dudando el periodo de diferentes cursos, cada uno de los cursos estaran diferenciados por su respectiva carpeta y nombre del curso 
```

## Para descargar el proyecto 
```shell
#Para usuarios que no sean propietarios o que no tengan configurado la ssh key
$ git clone git@github.com:KevyJQ/####.git
$ cd archivo
```

## Recordando comando de Git
```shell
  
  Para poder clonar el proyecto 
    $ git clone "proyect name"

  Para checar los cambios que realizamos 
    $ git status

  Para poner un comentario de lo que estas modificando 
    $ git commit 

  Para agregar los cambios realizados
    $ git add "Name"

  Para eliminar algo 
    $ git rm "Name"
  En dado caso que quieras forzar la eliminacion 
    $ git rm -r "Name"

  Para subir tus modificaciones nuevamente a git 
    $ git push origin "branch"

  Para checar el nombre de tu branch
    $ git branch

```
